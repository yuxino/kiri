name: Expanded 10x documentation demos
on:
  push:
    branches: [docs/demo-expanded-10x]
    paths: ['.github/workflows/expanded-demo.yml', 'docs/demos/capture/refine-expanded.py']
permissions:
  contents: read
  actions: read
concurrency:
  group: expanded-demo-capture
  cancel-in-progress: true
jobs:
  capture:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - uses: actions/download-artifact@v7
        with:
          github-token: ${{ github.token }}
          run-id: 33984702908
          pattern: demo-frontend-*
          path: docs/demos/capture/dist
      - name: Install documentation tools only
        run: |
          python -m pip install playwright==1.55.0 Pillow==11.3.0 reportlab==4.4.3
          python -m playwright install ffmpeg
          sudo apt-get install -y ffmpeg fonts-noto-cjk
      - name: Prepare reviewed capture scripts and original samples
        run: |
          python -I docs/demos/capture/refine-expanded.py
          python -I docs/demos/capture/fixtures.py
      - name: Record richer interactions at 10x with readable result holds
        run: python -I docs/demos/capture/expanded.py
      - name: Decode, verify and package all six videos and GIFs
        run: python -I docs/demos/capture/package-expanded.py
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: expanded-demo-review
          path: docs/demos/capture/expanded/
          if-no-files-found: error
          retention-days: 5
      - uses: actions/upload-artifact@v7
        with:
          name: expanded-demo-deliverables
          path: docs/demos/capture/expanded-deliverables/
          if-no-files-found: error
          retention-days: 7
